import * as React  from 'react'; 

// internal imports
import { NavBar } from '../sharedComponents';


export const Inventory = () => {
    return (
        <div>
            <NavBar />
            <h1 style={{marginTop: '100px', marginLeft: '100px'}}>Inventory</h1>
        </div>
    )
}